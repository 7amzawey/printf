#include "main.h"
/**
 * print_decimal: prints the decimal and return the
 * number of decimals to be printed
 * @decimal: the decimal number
 * Return: count
 */
int print_decimal(int decimal)
{
	_putchar(decimal);
	return (1);
}
